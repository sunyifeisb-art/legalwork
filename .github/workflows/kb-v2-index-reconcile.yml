name: KB v2 index reconcile staging

on:
  workflow_dispatch:

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - name: Remove legacy JSON index path and harden FTS reopen
        run: |
          python3 - <<'PY'
          import runpy
          runpy.run_path(
              'apps/desktop-legalwork/legalwork/src/knowledge/__kb_v2_final_polish_patch.py',
              run_name='__main__'
          )
          PY
